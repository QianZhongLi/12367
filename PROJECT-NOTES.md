# 港澳逗留计算器 — 需求变更总览

> 整理自全部对话历史  
> 最后更新：2026-06-01

---

## 一、计算逻辑修复

### 1.1 PDF 解析失败根因修复
- **问题**：`runCalculation` 默认累计起始日期 `${year}-09-01` 若晚于最早记录，会导致所有区间被过滤
- **修复**：当默认日期 > 最早记录时，取 `earliestDate`

### 1.2 自动检测地区逻辑修复
- **问题**：每次计算都根据口岸强行匹配地区，用户手动选择"其它留学生"后仍被切回
- **修复**：仅在**首次导入数据**时自动检测口岸分布；之后尊重用户手动选择
- **弹窗标题**：支持三种地区显示（澳门 / 香港 / 其它地区）

### 1.3 扣除规则重置 bug 修复
- **问题**：`getRules()` 在恢复默认 checkbox **之前**被调用，导致新计算读取残留状态
- **修复**：`const rules = getRules()` 移到恢复 checkbox 默认值之后

---

## 二、开学日期（累计起始日）

### 2.1 新增开学日期输入框
- 04 扣除规则区新增 `rulesStartDate`
- 与 07 结果区 `startDateInput`、09 万年历区 `calStartDate` **三向联动**

### 2.2 快捷按钮
- **设为9月1日**：取最早记录年份 → `YYYY-09-01`
- **设为1月15日（春季班）**：取最早记录年份 → `YYYY-01-15`
- 三处输入框后各配一组按钮

---

## 三、自动兜底逻辑

### 3.1 天数 < 100 自动去除扣除
- 计算结果 `< 100` 且未触发过 → 自动取消所有扣除勾选 → 重新计算

### 3.2 天数较少提示弹窗
- 结果弹窗后延迟 600ms 显示
- 提示"⚠️ 必须选择完整记录导出！仅导出近三月会导致计算结果不全"
- 配图 `5-1.jpg`，红色强调文字

### 3.3 自动切换地区链
- 弹窗结果为 0 时自动尝试：澳门 → 香港 → 其它留学生

---

## 四、新增功能模块

### 4.1 其它留学生模式
- 03 地区选择新增 **"其它留学生"** radio
- `selectedRegion === 'other'` 时所有口岸均视为有效，不计入复核

### 4.2 示例数据测试按钮
- 01 区新增按钮，一键填入 6 条横琴口岸记录并自动计算

### 4.3 10 被扣除记录列表
- 新增 section，列出：日期、星期、扣除原因、扣除天数
- 渲染函数 `renderDeductList()`
- 计算完成后**默认展开**
- 已去掉"有效天数"列

### 4.4 Formspree 反馈弹窗
- 三处反馈入口统一为弹窗按钮
- 表单 POST 到 `https://formspree.io/f/mgoqjvld`
- 自动附带**脱敏后的原始文本** + 问题描述（可选）
- 提交按钮显示"正在发送..."并禁用

---

## 五、隐私脱敏增强

- 身份证号、出生日期、姓名、CH 通行证编号、9位以上纯数字
- **新增**：`编号` 后的字母数字组合（如 `E260523247843`）

---

## 六、UI / 布局调整

### 6.1 Section 重排
- 最终顺序：01 → 02 → 03 → 04 → 05 计算结果 → 06 万年历 → 07 出入境总表 → 08 节假日明细 → 09 待复核口岸 → 10 被扣除记录
- 05~10 放在 `bottom-col` 单列容器，不与上方四宫格混合

### 6.2 统计数字迁移
- 识别记录数、完整离境区间数、待复核区间数、复核计入记录、复核新增天数
- 从 01/02 区域移到 **09 待复核口岸**区域内部

### 6.3 视图精简
- 注释掉月度局部视图、按月翻页按钮
- 年月翻页 toolbar 用 CSS `display: none` 隐藏（保留 DOM 防报错）

### 6.4 弹窗样式
- 结果弹窗：邮箱链接与"查看详情"按钮间增加 `margin-bottom:16px`
- 移动端自适应：`width: 100%`，不再挤出屏幕

### 6.5 表格移动端紧凑
- `min-width` 取消，`th/td padding` 缩小到 `8px 4px`，字体 `12px`

### 6.6 日历工具栏移动端换行
- `.calendar-extra-bar` 内部容器加 `flex-wrap: wrap`
- `.mode-btn`、`.calendar-toolbar button` 移动端缩小 padding/font-size

### 6.7 合计行颜色
- 从刺眼纯红 `#dc2626` 改为淡红背景 `#fee2e2` + 深红文字 `#991b1b`

### 6.8 折叠标题文字动态切换
- 折叠状态 → "点击展开"
- 展开状态 → "点击折叠"
- 10 区域默认展开后标题同步为"点击折叠"

### 6.9 JS 不可用警告
- 从右上角悬浮窗改为**底部固定通栏**

---

## 七、交互优化

### 7.1 滚动动画只做一次
- 新增 `hasScrolledAfterCalc` 标志
- 首次计算后：关闭弹窗 → 滚动到结果区 → 3秒后滚动到日历
- 再次计算/重新计算不再自动滚动

### 7.2 开头折叠动画保留
- 页面加载1秒后自动折叠 hero 与 PDF 指南
- 旋转倒三角箭头

---

## 八、构建脚本改进

### 8.1 图片内联
- 从固定 1~10.jpg 改为**遍历 `截图` 目录下所有 `.jpg`**
- `5-1.jpg` 也被正确压缩并内联

### 8.2 自动备份
- build 完成后自动在 `backup/` 目录创建时间戳备份
- 文件名格式：`index_YYYYMMDD_HHMMSS.html` / `.zip`

---

## 九、技术栈

- 纯前端 HTML + JS，单文件部署（兼容 `file://`）
- 构建脚本：`build_single_file.py` → 输出 `index.html` + `index.zip`
